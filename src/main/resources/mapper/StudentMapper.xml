<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.StudentMapper">

    <select id="selectStudentPage" resultType="com.example.demo.entity.Student">
        SELECT * FROM student
        WHERE 1=1
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="studentNo != null and studentNo != ''">
            AND student_no LIKE CONCAT('%', #{studentNo}, '%')
        </if>
        <if test="classId != null">
            AND class_id = #{classId}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectRecentStudents" resultType="com.example.demo.entity.Student">
        SELECT * FROM student
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectCountByClassId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM student WHERE class_id = #{classId} AND status = 1
    </select>

</mapper>
